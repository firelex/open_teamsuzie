{
  "name": "@teamsuzie/crypto",
  "version": "0.1.0",
  "description": "Zero-dependency AES-256-GCM + key-hash helpers shared by Team Suzie services. Extracted so callers (PE settings host, shared-auth, future thin services) don't each duplicate byte-compatible crypto.",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3",
    "vitest": "^4.0.18"
  }
}
