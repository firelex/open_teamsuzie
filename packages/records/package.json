{
  "name": "@teamsuzie/records",
  "version": "0.1.0",
  "description": "SQLite schema + store for org-scoped structured records with arbitrary JSON custom fields. Generic over subject domain — host apps define record types at runtime (e.g. 'matter', 'asset', 'lead', 'ticket'). No CRM assumptions baked in.",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@teamsuzie/db-sqlite": "workspace:*"
  },
  "devDependencies": {
    "@teamsuzie/activity": "workspace:*",
    "@teamsuzie/pipelines": "workspace:*",
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3"
  }
}
