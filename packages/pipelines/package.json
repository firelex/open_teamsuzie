{
  "name": "@teamsuzie/pipelines",
  "version": "0.1.0",
  "description": "SQLite schema + store for stage-based pipelines over opaque (subject_type, subject_id) refs. Generic over the domain — same shape serves sales, hiring, support, procurement, onboarding, legal intake. Tracks current stage placements plus an append-only transition history.",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@teamsuzie/db-sqlite": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3"
  }
}
