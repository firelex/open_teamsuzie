"""Template-injection Word agent.

Composes .docx documents by filling content into the named bracket-tokens
(or content-control tags) of an existing template, leaving the template's
styles, letterhead, headers/footers and section structure intact."""

__version__ = "0.1.0"
