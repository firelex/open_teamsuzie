PPTX_TEMPLATE_AGENT_PORT=8081
PPTX_TEMPLATE_AGENT_OUTPUT_DIR=./output
PPTX_TEMPLATE_AGENT_TEMPLATE=./templates/ic-template-v3.pptx
PPTX_TEMPLATE_AGENT_PUBLIC_BASE_URL=http://localhost:8081

# LLM configuration source. The agent resolves {base_url, api_key, model}
# from pe-settings-host on every generate call and POSTs directly to the
# upstream's OpenAI-compatible /chat/completions. Configure the active
# provider via the Settings UI.
PE_SETTINGS_HOST_URL=http://localhost:19271

# Webhook delivery (optional)
ADMIN_SERVICE_URL=
