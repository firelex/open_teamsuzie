{
  "name": "@teamsuzie/starter-external-agent",
  "version": "0.1.0",
  "description": "Starter for an external marketplace agent: chatbot + custom tools, registers with the suzie_monorepo mothership via @teamsuzie/platform-bridge",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "cd ../../../packages/approvals && npx tsc && cd ../skills && npx tsc && cd ../agent-loop && npx tsc && cd ../platform-bridge && npx tsc",
    "dev": "tsx watch src/index.ts & pnpm --filter @teamsuzie/starter-external-agent-client dev & wait",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@teamsuzie/agent-loop": "workspace:*",
    "@teamsuzie/approvals": "workspace:*",
    "@teamsuzie/chats": "workspace:*",
    "@teamsuzie/db-sqlite": "workspace:*",
    "@teamsuzie/markdown-document": "workspace:*",
    "@teamsuzie/personas": "workspace:*",
    "@teamsuzie/platform-bridge": "workspace:*",
    "cors": "^2.8.5",
    "dotenv": "^16.6.0",
    "express": "^5.1.0",
    "multer": "^2.0.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.3",
    "@types/multer": "^2.0.0",
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
